export const WIDTH = 800;
export const HEIGHT = 600;

export const PUCK_RADIUS = 12;
export const MAX_POWER = 600;

export const BASE_FRICTION = 0.98; // slowing down
export const STOP_THRESHOLD = 5; // when velocity is "stopped"